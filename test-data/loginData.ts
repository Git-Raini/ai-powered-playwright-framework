import { ENV } from '../utils/env';

export const loginData = {

    validUsername: ENV.ORANGE_HRM_USERNAME,

    validPassword: ENV.ORANGE_HRM_PASSWORD,

    invalidUsername: 'invalidUser',

    invalidPassword: 'invalidPassword'

};